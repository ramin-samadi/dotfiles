#!/usr/bin/env bash

CURRENT_CLIPBOARD=$(xsel -ob)
NOTIFICATION=$(echo "$CURRENT_CLIPBOARD")

notify-send "QRCode Generated" "$NOTIFICATION"
qrencode -t PNG -s 8 -o /tmp/tmp.png "$CURRENT_CLIPBOARD"
sxiv /tmp/tmp.png
