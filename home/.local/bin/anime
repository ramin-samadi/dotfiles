#!/bin/bash

# NOTE: st ani-cli dmenu

TERMINAL="st"
ANIME_NAME=$(echo -n "" | dmenu -p " Anime: ")

execute_ani_cli() {
	"$1" -e bash -c "ani-cli \"$2\" && pkill -P $$"
}

if [[ -z "${ANIME_NAME// /}" ]]; then
	exit
else
	execute_ani_cli "${TERMINAL}" "${ANIME_NAME}"
fi
