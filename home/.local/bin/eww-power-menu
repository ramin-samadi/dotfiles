#!/bin/bash

quick_settings="/home/$USER/.local/bin/.quick_settings"
status=$(cat -q "$quick_settings")

if [[ "${status}" = true ]]; then
	eww close actions
	status=false
	echo "${status}" >"$quick_settings"
else
	eww open actions
	status=true
	echo "${status}" >"$quick_settings"
fi
