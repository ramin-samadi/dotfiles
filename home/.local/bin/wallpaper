#!/usr/bin/env bash

SELECTED_WALLPAPER=$(ls /home/$USER/.wallpaper/ | dmenu -p "")

if [[ -z "${SELECTED_WALLPAPER// /}" ]]; then
	exit
else
	feh --bg-fill /home/$USER/.wallpaper/"$SELECTED_WALLPAPER"
fi
