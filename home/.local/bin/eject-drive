#!/bin/bash

# NOTE: gawk dmenu sudo

SELECTED_DRIVE=$(lsblk -d -p | awk '$3 == "1" {print $1}' | sed 's|^/dev/||' | dmenu -fn "Meslo LGS Nerd Font Mono:size=20:bold" -nb "#1E1E2E" -sb "#313244" -sf "#cdd6f4" -nf "#a6adc8" -p "")

udisksctl power-off -b "/dev/${SELECTED_DRIVE}"
