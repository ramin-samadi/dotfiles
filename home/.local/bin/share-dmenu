#!/usr/bin/env bash

SELECTED_FILE=$(find $HOME -type f 2>/dev/null | sed 's/\/home\/ramin\///g' | dmenu -i -p "")

if [[ -z "${SELECTED_FILE// /}" ]]; then
	exit
else
	curl -sF "file=@$HOME/$SELECTED_FILE" 0x0.st | tr -d '\n' | xclip -selection clipboard
	qrclip
fi
