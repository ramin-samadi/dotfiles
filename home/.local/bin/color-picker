#!/usr/bin/env bash

# NOTE: colorpicker xclip libnotify

color=$(colorpicker --short --one-shot --preview | tr -d '\n')

pick_color() {
	if [ -n "$color" ]; then
		printf '%s' "$color" | xclip -selection clipboard
		notify-send " $color"
	fi
}

pick_color
